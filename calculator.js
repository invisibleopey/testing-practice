const calculator = {
  add(x, y) {
    return Number(x) + Number(y);
  },
  subtract(x, y) {
    return Number(x) - Number(y);
  },
  divide(x, y) {
    return x / y;
  },
  multiply(x, y) {
    return x * y;
  },
};
export default calculator;
